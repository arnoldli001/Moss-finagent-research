# 会话交接 · 2026-09-14

> 用途：把 2026-09-13 在 moss-finance-assistant 窗口中完成的工作上下文迁移到本项目对话。
> 新对话首条消息可直接说：「读 docs/HANDOFF_20260914.md 接续上下文」。

## 一、本项目（Moss-finagent-research）2026-09-13 已完成并提交

工作树干净，测试基线 **196 passed**，ruff 零告警。提交链（新→旧）：

- `cda1025` perf(backtest): 回测全历史行情10分钟TTL缓存（实测 2.8s→0.01s，响应 `cache` 字段 + 前端徽标）
- `ab4bf10` style: 审计写入合并 with、测试文件句柄上下文管理器
- `85bb4e2` chore: 清理陈旧 8000 端口与失效脚本引用（setup_dev/seed_data/src.worker；vite/DEPLOYMENT 统一 8100）
- `301a8ea` docs+chore: seed_demo_data.py 幂等预热 + API_REFERENCE/README 整理
- `7364f49` feat(demo): scripts/demo_tour.py 七步演示导览（实跑 9/9 PASS）
- `5749d6f` feat(backtest): POST /api/v1/backtest/run + 前端「策略回测」页（SVG净值曲线/风险表/免责声明）

更早的回测引擎本体（src/backtest/：signals/engine/data）见 `92e8ace` 及路线图第7周。

### 路线图状态（docs/DEVELOPMENT_ROADMAP.md）

- 第7周：**全部完成**（回测、demo_tour、seed_demo_data、文档）
- 第8周：性能优化、代码清理已完成；**演示彩排**待人工跑 `uv run python scripts/demo_tour.py`；
  **最终交付**依赖 B02
- Backlog：B01 付费产业接口（等Key，保持indicator id不变只换连接器）；B02 GitHub（ci.yml就绪，待用户建库 remote add+push）

### 关键环境约定（勿踩坑）

- 服务：`$env:PYTHONPATH="."; uv run uvicorn src.api.main:app --port 8100`（8000被veighna占用）
- 重启先 `netstat -ano | findstr :8100 | findstr LISTENING` 杀 PID
- PowerShell 5：禁 `&&`/`||`/heredoc；pytest 8.3 线锁定；conftest 用 tmp_dir 不用 tmp_path（Windows清理慢）
- 真实回测实测结论（601088，2007-10~2025-08，213月）：朴素PPI动量规则累计 -46.09% vs 买入持有 +20.38%，
  超额 -66.47%——**框架成立但该规则无超额收益**，对外必须诚实披露+风险声明

## 二、新建的独立姊妹项目：d:\code\backtest-engine（与本项目无代码依赖）

用户要求：沿用本项目回测引擎设计，但数据源改**本地SQLite**、独立项目（不放moss/finagent）、自带前端。

- 独立 git 仓库，提交：`7827efe`（初始化）、`bb1de6a`（PE估值表+CSV导入）
- 结构：`backtest/`（signals.py 趋势+PE闸门严格扩展窗口、engine.py 命中率/净值/回撤/夏普、data.py 发布月对齐）、
  `db/`（SQLite：macro_data / stock_price / valuation）、`api/`（FastAPI，8100端口，/backtest/run + /catalog）、
  `web/`（Vite+React+TS，纯SVG净值曲线，深色主题）、`scripts/`（seed_demo.py 合成种子、import_csv.py）、tests/
- **25 passed，0.5s**（conftest 的 tmp_dir 夹具解决了 tmp_path Windows 清理慢）
- PE 闸门已真实接通：valuation(code,period_date,pe) 按月归并；无PE数据自动不触发，前端绿/橙提示。
  实测 PE≤15：看多 36→20、持仓 35→19 月、超额 -4.61%→+0.61%（合成数据）
- CSV 导入：`--kind macro|price|valuation`，追加保留+UPSERT同键覆盖、中英文列别名、
  `--code/--indicator` 补缺省键、非法行跳过报行号；实测幂等
- 数据为 crc32 确定性合成（无市场含义），真实数据走 CSV 导入，schema 不变
- 注意：该独立项目与 Moss-finagent-research 的 src/backtest 是**设计同源、各自演进**，后续任一侧改动不会自动同步
